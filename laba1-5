#include <stdio.h>

int main() {
    int N;
    printf("Введите число N (1 ≤ N ≤ 26): ");
    scanf("%d", &N);
    
    for (int i = 0; i < N; i++) {
        printf("%c ", 'z' - i);
    }
    printf("\n");
    return 0;
}
