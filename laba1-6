#include <stdio.h>

int main() {
    char C;
    
    scanf("%c", &C);
    
    if (isdigit(C)) {
        printf("digit\n");
    }
    else if (isalpha(C)) {
        printf("lat\n");
    }
    else {
        printf("Не буква и не цифра\n");
    }
    
    return 0;
}
